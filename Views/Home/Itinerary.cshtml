@model TravelPlanner.Models.Trip
@{
    ViewData["Title"] = "Trip Itinerary";
}

<div class="container py-5 fade-in">
    <!-- Trip Header -->
    <div class="text-center mb-4">
        <h2 class="fw-bold text-primary">🌍 @Model.Name - @Model.Destination</h2>
        <p class="text-muted">
            @Model.StartDate:dd MMM yyyy → @Model.EndDate:dd MMM yyyy
        </p>
        <a asp-action="Dashboard" class="btn btn-outline-secondary btn-sm rounded-pill">← Back to Dashboard</a>
    </div>

    <!-- Upload Images -->
    <div class="card shadow-lg border-0 rounded-4 p-4 mb-4 bg-white">
        <h5 class="text-success mb-3">📸 Upload Trip Images</h5>
        <form asp-action="UploadTripImage" method="post" enctype="multipart/form-data">
            <input type="hidden" name="TripId" value="@Model.TripId" />
            <div class="d-flex gap-2 align-items-center">
                <input type="file" name="file" class="form-control shadow-sm rounded-pill" required />
                <button type="submit" class="btn btn-gradient shadow-sm rounded-pill">Upload</button>
            </div>
        </form>
    </div>

    <!-- Trip Images Gallery -->
    @if (Model.Images != null && Model.Images.Any())
    {
        <div class="mb-4">
            <h5 class="fw-semibold text-primary mb-2">📸 Trip Images</h5>
            <div class="d-flex flex-wrap gap-3">
                @foreach (var img in Model.Images)
                {
                    <div class="position-relative fade-in">
                        <img src="@img.ImageUrl" class="rounded-4 shadow-sm" style="width:150px; height:100px; object-fit:cover;" />
                        <form asp-action="DeleteTripImage" method="post" class="position-absolute top-0 end-0">
                            @Html.AntiForgeryToken()
                            <input type="hidden" name="id" value="@img.TripImageId" />
                            <button type="submit" class="btn btn-sm btn-danger rounded-circle p-1 shadow-sm" onclick="return confirm('Delete this image?');">
                                <i class="bi bi-x"></i>
                            </button>
                        </form>
                    </div>
                }
            </div>
        </div>
    }

    <!-- Add Itinerary Form -->
    <div class="card shadow-lg border-0 rounded-4 p-4 mb-5 bg-white fade-in">
        <h4 class="text-success mb-3">🗓️ Add New Itinerary Item</h4>
        <form asp-action="AddItineraryItem" method="post">
            @Html.AntiForgeryToken()
            <input type="hidden" name="TripId" value="@Model.TripId" />
            <div class="row g-3">
                <div class="col-md-4">
                    <label class="form-label fw-semibold">Activity Name</label>
                    <input type="text" name="Name" class="form-control shadow-sm rounded-pill" required />
                </div>
                <div class="col-md-3">
                    <label class="form-label fw-semibold">Type</label>
                    <select name="Type" class="form-select shadow-sm rounded-pill" required>
                        <option value="">Select Type</option>
                        <option value="Flight">✈️ Flight</option>
                        <option value="Hotel">🏨 Hotel</option>
                        <option value="Activity">🎯 Activity</option>
                        <option value="Meal">🍽️ Meal</option>
                        <option value="Transport">🚗 Transport</option>
                    </select>
                </div>
                <div class="col-md-3">
                    <label class="form-label fw-semibold">Date</label>
                    <input type="date" name="Date" class="form-control shadow-sm rounded-pill" required />
                </div>
                <div class="col-md-12">
                    <label class="form-label fw-semibold">Notes</label>
                    <textarea name="Notes" class="form-control shadow-sm rounded-4" rows="2" placeholder="Add details..."></textarea>
                </div>
                <div class="col-md-12 text-end">
                    <button type="submit" class="btn btn-gradient px-4 mt-2 rounded-pill">+ Add Item</button>
                </div>
            </div>
        </form>
    </div>

    <!-- Planned Activities -->
    <h4 class="fw-bold mb-4 text-primary">📅 Planned Activities</h4>

    @if (Model.Itineraries == null || !Model.Itineraries.Any())
    {
        <div class="alert alert-light text-center border rounded-4 py-4 shadow-sm fade-in">
            <i class="bi bi-calendar-x fs-1 text-muted"></i>
            <h5 class="mt-3">No itinerary items yet</h5>
            <p class="text-muted">Start adding your trip activities!</p>
        </div>
    }
    else
    {
        <div class="row g-4">
            @foreach (var item in Model.Itineraries)
            {
                <div class="col-md-6 col-lg-4 fade-in">
                    <div class="card shadow-sm border-0 rounded-4 h-100 hover-shadow">
                        <div class="card-body">
                            <h5 class="text-primary fw-bold">@item.Name</h5>
                            <p class="text-muted mb-1">@item.Type • @(item.Date.HasValue ? item.Date.Value.ToString("dd MMM yyyy") : "")</p>
                            <p class="small text-secondary">@item.Notes</p>
                        </div>
                        <div class="card-footer bg-white border-0 d-flex justify-content-between">
                            <a asp-action="EditItineraryItem" asp-route-id="@item.ItineraryId" class="btn btn-outline-info btn-sm rounded-pill shadow-sm">Edit</a>
                            <form asp-action="DeleteItineraryItem" asp-route-id="@item.ItineraryId" method="post" onsubmit="return confirm('Delete this item?');">
                                @Html.AntiForgeryToken()
                                <button type="submit" class="btn btn-outline-danger btn-sm rounded-pill shadow-sm">Delete</button>
                            </form>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
</div>

<style>
    body {
        background: linear-gradient(135deg, #e3f2fd, #ffffff);
        font-family: "Poppins", sans-serif;
    }

    .btn-gradient {
        background: linear-gradient(135deg, #4facfe, #00f2fe);
        color: white;
        border: none;
        transition: all 0.3s ease;
    }

    .btn-gradient:hover {
        transform: translateY(-3px);
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
    }

    .fade-in {
        animation: fadeInUp 0.6s ease forwards;
        opacity: 0;
        transform: translateY(20px);
    }

    @@keyframes fadeInUp {
        from { opacity: 0; transform: translateY(20px); }
        to { opacity: 1; transform: translateY(0); }
    }
</style>
